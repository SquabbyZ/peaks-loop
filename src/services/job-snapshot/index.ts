/**
 * peaks-loop-job-snapshot area (consolidated into peaks-loop).
 *
 * Public surface: runSnapshot, captureResources, and the
 * ResourceSnapshot type.
 */
export { collectResourceSnapshot } from './snapshot.js';
export type { ResourceSnapshot } from './types.js';
