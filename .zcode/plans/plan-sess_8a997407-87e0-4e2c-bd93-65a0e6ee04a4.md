## Plan — fix 15 narrative failures + 1 cli timeout by rewriting both READMEs' hero section + bumping cli-command-branches timeout

### Root-cause diagnosis (Phase 1 complete)

| Test | Root cause | Fix scope |
|---|---|---|
| `tests/unit/narrative/loop-engineering-positioning.test.ts` (15 failures) | The M9 narrative spec at `docs/superpowers/specs/2026-07-07-...md` defines `peaks-loop` as a "Loop Engineering crystallization system" with a four-layer asset model (Loop Engineering Asset + Bee Asset + Workflow Trace + Evolution Evaluation), karpathy × darwin dual discipline, and `peaks-code` scoped to code-domain only. The READMEs were never rewritten to match — they still lead with "五域编排系统" / "five-domain orchestrator" framing. | Rewrite the hero section of **both** `README.md` (zh) and `README-en.md`. |
| `tests/unit/cli-command-branches.test.ts` (1 timeout) | The test "reports failed doctor and skill doctor checks" pins a per-test `timeout: 10_000` (line 95) but does two `runRegisteredCommand(registerCoreAndArtifactCommands, …)` calls — the first call's dynamic `await import('../../src/cli/commands/core-artifact-commands.js')` cold-loads ~10 transitive modules + service files, which combined with prior tests in the single-worker fork blows the 10s ceiling. | Bump the per-test timeout from `10_000` → `30_000`. |
| `tests/unit/services/filesystem/file-lock.test.ts` | Passes alone (8/8), passes in any pair. False alarm in the original bulk run — vitest reporter interleaved output. | **No fix needed.** |
| `tests/unit/services/job/job-resource-snapshot.test.ts` | Passes alone (12/12), passes in any pair. Same false-alarm cause. | **No fix needed.** |

### Files to edit

1. **`C:\Users\smallMark\Desktop\peaks-loop\README.md`** (Chinese, primary)
2. **`C:\Users\smallMark\Desktop\peaks-loop\README-en.md`** (English)
3. **`C:\Users\smallMark\Desktop\peaks-loop\tests\unit\cli-command-branches.test.ts`** (bump one timeout)

### README.md (zh) — change summary

**Insert a new section `## 它是什么` immediately after the demo GIF block (between the `---` separator and the existing `## 30 秒上手` section).**

The hero section will:
- Open with "peaks-loop 是一个 **Loop Engineering 结晶系统**,不是一个工作流工具" (matches `/Loop Engineering\s*结晶系统|loop engineering\s*的工程实现/` AND `/不是工作流工具/`)
- Contain literal `Loop Engineering` (matches `expect(hero).toContain('Loop Engineering')`)
- Match `/loop engineering/i` (lowercase fallback)
- Present a 4-row table with the four-layer asset model using literal phrases:
  - "Loop Engineering 资产" row
  - "Bee 资产" row (matches `/Bee\s*资产|Bee Asset/`)
  - "Workflow Trace (执行轨迹)" row (matches `/Workflow Trace|执行轨迹/`)
  - "Evolution Evaluation (反漂移)" row (matches `/Evolution Evaluation|反漂移/`)
- Contain a bullet line "**工程化规则 = karpathy 风格 · 验证每次改进 = darwin 风格**" (matches `/karpathy/` AND `/darwin/`)
- **NOT** match `/19\s*(个|skills?)/i` (avoid enumerating the 19 skill names here)

**Update the existing `/peaks-code 帮我熟悉下当前的项目` line in `## 30 秒上手`** to read `/peaks-code 帮我熟悉下当前的项目(代码域)` or similar so that the regex `/peaks-code[\s\S]{0,40}code-domain|code-domain[\s\S]{0,80}peaks-code/` passes. Simpler: change the description from `帮我实现这个功能` to use a phrasing that keeps `peaks-code` and `code-domain` within 40 chars — e.g. on the second code-row entry replace `帮我实现这个功能` with `(code-domain)` annotation, OR add `(code-domain only)` after one peaks-code mention. **Cleanest minimal change:** in the "What it does for you" table, change the row `💻 **代码** | `/peaks-code 帮我实现这个功能` | PRD → RD → ...` to `💻 **代码域 (code-domain) only** | `/peaks-code 帮我实现这个功能` | PRD → RD → ...`. That puts `code-domain` and `peaks-code` within 40 chars in the same line.

**Add the two spec references** in a new "## 设计与规范" footnote or as a line in an existing footer section. Simplest: add them as two lines inside the existing `## 链接` section, e.g.:
- 沉淀设计 → [`docs/superpowers/specs/2026-07-04-peaks-maker-dynamic-skill-sediment-design.md`](./docs/superpowers/specs/2026-07-04-peaks-maker-dynamic-skill-sediment-design.md)
- Loop Engineering 结晶设计 → [`docs/superpowers/specs/2026-07-07-peaks-loop-loop-engineering-crystallization-design.md`](./docs/superpowers/specs/2026-07-07-peaks-loop-loop-engineering-crystallization-design.md)

### README-en.md — change summary

Mirror the zh changes:
- Insert `## What it is` between the GIF block and `## Up and running in 30 seconds`, with:
  - "peaks-loop is a **Loop Engineering crystallization system**, not a workflow tool." (matches `/Loop Engineering\s*crystallization system|Loop engineering, engineered/` AND `/not a workflow tool/i`)
  - Contains literal `Loop Engineering`
  - Matches `/loop engineering/i`
  - 4-row table using literal phrases:
    - "Loop Engineering Asset"
    - "Bee Asset"
    - "Workflow Trace"
    - "Evolution Evaluation"
  - Bullet: "**Engineering principles = karpathy-style · Verify every improvement = darwin-style**"
- Update the `💻 **Code**` row in the existing table to `💻 **Code (code-domain) only**` (puts `code-domain` and `peaks-code` within 80 chars)
- Add the two spec doc references in the `## Links` section

### tests/unit/cli-command-branches.test.ts — change summary

Change line 95 from:
```ts
}, 10_000);
```
to:
```ts
}, 30_000);
```

### Acceptance check

After edits, run:
```bash
npx vitest run tests/unit/narrative/loop-engineering-positioning.test.ts
npx vitest run tests/unit/cli-command-branches.test.ts tests/unit/narrative/loop-engineering-positioning.test.ts tests/unit/services/filesystem/file-lock.test.ts tests/unit/services/job/job-resource-snapshot.test.ts
```

Expected: 52/52 pass.

Then a wider smoke (no full suite — that's expensive):
```bash
npx vitest run tests/unit/narrative tests/unit/cli-command-branches.test.ts tests/unit/services/filesystem tests/unit/services/job
```

### Risk

Low — both READMEs are human-facing documents; this only adds a new top section and tweaks the code-row label + adds spec links in the footer. No code, schema, or test contract changes. The timeout bump is the smallest possible change to absorb the cold-import cost.

### Out of scope

- Will NOT touch the spec files (`docs/superpowers/specs/2026-07-07-...md`, `2026-07-04-...md`), ADR 0007, or peaks-code/SKILL.md — the narrative test for those files already passes.
- Will NOT run the full `pnpm test` (5,439 cases — out of scope for a targeted fix).