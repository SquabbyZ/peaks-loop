---
name: capability-baseline-4-0-8-freeze
description: 2026-08-03 session — peaks-loop 4.0.8 capability baseline frozen (15 P0 journeys, user-approved via AskUserQuestion, freeze commit d92d8c5 candidate → 4.0.8 frozen via `peaks baseline freeze`).
metadata:
  type: project
---

**What happened**: Task 6 of 5-slice plan wrote the candidate input (commit d92d8c5, 452-line JSON, 15 rows). User confirmed freeze via `AskUserQuestion` (Human-NL-Choice-Only). Task 7 ran `peaks baseline freeze --from openspec/baselines/inputs/4.0.8-baseline-input.json --project .` which wrote `openspec/baselines/current/capability-baseline.json` + `.lock` and a byte-identical snapshot at `openspec/baselines/history/4.0.8/`.

**Why**: This freezes 15 P0 user journeys' product semantics so future slices can use the C-plan guard runner / audit service to detect drift. Without this freeze, the guard runner has no baseline to compare against.

**How to apply**:
- Any future change to a J01..J15 product behavior must be paired with `peaks baseline freeze-update` (Human-NL-Choice-Only, two-step confirmation + passphrase) — NOT silently.
- `peaks baseline diff` reads `current/` and shows divergence from a hypothetical "now" behavior.
- `peaks baseline audit` (Slice 5) cross-checks current/ against guard results.
- To undo this freeze, use `peaks baseline rollback --to 4.0.8` (already at 4.0.8; rollback semantics: revert to a prior `history/<ver>/` snapshot if needed in the future).
