# TXT Handoff: slice 2026-06-04-monorepo-and-release

- session: 2026-06-04-session-cda1cd
- rid: 2026-06-04-monorepo-and-release
- mode: full-auto
- completed: 2026-06-04 ~21:45 GMT+8

## What was done

User invoked peaks-solo against this repo, then asked to dogfood today's 17 commits on `c:/Users/smallMark/Desktop/peaksclaw/ice-cola`. Round 1 (global `peaks` binary, v1.2.8) revealed the global CLI was stale vs local source. Round 2 (local source via `pnpm exec tsx src/cli/index.ts`) revealed `peaks scan libraries` was monorepo-blind (returned 1 lib on ice-cola's 7-package monorepo). User accepted three options: (A) full RD slice to fix the monorepo defect, (B) chore to bump version + add pnpm install note, (C) issue file as deferred backlog. All three were executed.

## Final state

| Slice | Type | Commit | Files | Status |
|---|---|---|---|---|
| C | docs | `4d3e18a` | 1 file (`sc/issue-001-monorepo-blind.md`) | landed |
| A | feature | `d3e314c` | 3 files (service + types + tests) | landed, QA verdict = pass |
| B | chore | `69cc1f7` (amended from `8a88ac4`) | 3 files (package.json + README.md + src/shared/version.ts) | landed |
| (post-slice) | meta | `51b9cae` | 12 files (cda1cd session artifacts) | landed per "提交中间产物" pattern |

## Slice A evidence summary

- **Unit tests**: 22/22 pass in 374ms (8 `parseMajorVersion` + 14 prior `scanLibraries` + 7 new monorepo cases).
- **Typecheck**: clean.
- **Dogfood on ice-cola**: `data.totalCount: 202` (was 1), `data.workspaces.length: 7`, `data.warnings: []`.
- **Back-stops**: `peaks skill runbook peaks-solo` still `peaksCommandCount: 31`; `peaks scan request-type-sanity --type feature` returns `consistent: true`.
- **Regression sweep**: 1809 pass / 7 pre-existing Windows failures (verified against parent commit `92ef8c3`; not introduced by this slice) / 9 skip.
- **Security**: 0 CRITICAL/HIGH/MEDIUM, 3 LOW (inherited from pre-existing read-only service; out of scope).
- **Performance**: 1.447s real on the dogfood, 3.46× under the 5s gate.

## Slice B evidence summary

- `package.json` version: 1.2.8 → 1.2.9.
- `README.md` gained `## 本地开发（从源码跑 CLI）` section (12 lines, 1 code block, no marketing).
- `src/shared/version.ts` regenerated by `scripts/sync-version.mjs` and included in the amended commit (see "Workflow gotcha" below).

## Validated decisions

- **D1** — `peaks scan libraries` monorepo detection order: `pnpm-workspace.yaml` (preferred) → `package.json` `workspaces` (npm or yarn object) → `lerna.json` `packages` → single-package fallback. pnpm-wins precedence when both pnpm and npm `workspaces` are present. Hand-rolled glob matcher; no new dependencies.
- **D2** — Single-package projects produce `workspaces: []` (additive; back-compat preserved by 14 byte-identical pre-existing tests).
- **D3** — Aggregate by default (`libraries[]`, `totalCount`, `byScope` are summed across all workspaces). No `--per-workspace` mode in this slice.
- **D4** — Version bumped to 1.2.9 (per the user's original option text), not 1.3.0, even though slice A is a feature. The next minor (1.3.0) is reserved for the user's chosen Trae adapter work or the next round of peaks-* iterations.

## Workflow gotchas surfaced by this session (these are real)

- **W1** — Global `peaks` CLI is stale by ~24h vs local source. Confirmed by the round-1 vs round-2 dogfood diff. The `peaks --version` is still 1.2.8 in the global install until a `pnpm build && npm install -g` cycle runs. The fix in slice B (README pnpm install note) addresses the on-ramp issue, but the global-stale issue is a recurring one and is now baked into the workflow.
- **W2** — `peaks --version` does NOT read from `package.json` directly; it reads from `src/shared/version.ts` which is generated by `scripts/sync-version.mjs`. A version bump in `package.json` alone is invisible to the CLI until the sync script runs. We amended the slice B commit to include the regenerated `src/shared/version.ts` after the first run reported `1.2.8` post-bump. A future improvement: make `sync-version.mjs` a `prepare` / `prebuild` / postinstall hook so it always runs.
- **W3** — Every `peaks workspace init` in the same peaks-cli session creates a new session directory. This session bloat (3 untracked dirs: c72760, 4f7f6e, 4ead08) is left over from intermediate operations and could be cleaned up. The session-bloat is a real workflow defect: a single user-driven workflow should not produce 4+ session dirs. Queued as a future slice.
- **W4** — `peaks sc validate` and `peaks sc boundary` failed in this session because `.peaks/.session.json` points to an ancient session (b60252, Mac path) and the active session (4f7f6e) doesn't see the cda1cd artifacts. This is a session-binding drift issue; the QA verdict (authoritative) was pass, so the SC failure is a tooling defect, not a slice defect.

## Standards deltas (peaks standards preflight)

- `CLAUDE.md` and `.claude/rules/**`: no changes. Pre-existing standards in the peaks-cli repo are the source of truth and remain applicable.
- One runtime environment finding: fresh `git clone` of peaks-cli needs `pnpm install` before `tsx src/cli/index.ts` works. The README now documents this. No change to the dev-preference.md rule set; this is a runtime onboarding step, not a coding standard.

## Open questions (carry into the next iteration)

- **Q1** — Should peaks-cli ship a `peaks workspace reconcile` command that consolidates the multiple-session-dir bloat into a single canonical session? This would close W3 and unblock the SC-validate failure mode in W4.
- **Q2** — Should `scripts/sync-version.mjs` be promoted to a `predev` / `prepack` script so `pnpm dev` and `pnpm pack` always start with a fresh `src/shared/version.ts`? This would close W2 and prevent the "package.json bumped but CLI still reports old version" failure mode in future releases.
- **Q3** — Trae adapter (per the prior-session S54 plan) is the next roadmap item per the user. The SPEC.md § Swarm v0.1 is the prerequisite per S54. This session did NOT advance that work; it consumed the user's bandwidth on a peaks-cli self-fix. The next session should resume SPEC.md § Swarm or jump to the Trae adapter MVP if the user prefers.

## Next action for the user

```bash
# In c:/Users/smallMark/Desktop/peaks-cli:
pnpm build && npm install -g    # promote the new bits to the global peaks CLI
peaks scan libraries --project c:/Users/smallMark/Desktop/peaksclaw/ice-cola --json   # should now show 202 libs / 7 workspaces from the global install
```

If the build + install cycle confirms the global install now matches source, the work is releasable. Otherwise, file a follow-up slice addressing W1/W2.

## Artifact paths (canonical)

- PRD: `.peaks/2026-06-04-session-cda1cd/prd/requests/001-2026-06-04-monorepo-and-release.md`
- RD request: `.peaks/2026-06-04-session-cda1cd/rd/requests/001-2026-06-04-monorepo-and-release.md`
- RD tech-doc: `.peaks/2026-06-04-session-cda1cd/rd/tech-doc.md`
- RD code-review: `.peaks/2026-06-04-session-cda1cd/rd/code-review.md`
- RD security-review: `.peaks/2026-06-04-session-cda1cd/rd/security-review.md`
- QA request: `.peaks/2026-06-04-session-cda1cd/qa/requests/001-2026-06-04-monorepo-and-release.md`
- QA test-cases: `.peaks/2026-06-04-session-cda1cd/qa/test-cases/2026-06-04-monorepo-and-release.md`
- QA test-report: `.peaks/2026-06-04-session-cda1cd/qa/test-reports/2026-06-04-monorepo-and-release.md`
- QA security findings: `.peaks/2026-06-04-session-cda1cd/qa/security-findings.md`
- QA performance findings: `.peaks/2026-06-04-session-cda1cd/qa/performance-findings.md`
- Issue file (slice C): `.peaks/2026-06-04-session-cda1cd/sc/issue-001-monorepo-blind.md`
- TXT handoff (this file): `.peaks/2026-06-04-session-cda1cd/txt/handoff.md`

---

<!-- peaks-memory:start kind=decision -->
peaks scan libraries is now monorepo-aware. Detection order is pnpm-workspace.yaml (preferred) → package.json workspaces field (npm or yarn) → lerna.json packages → single-package fallback. Hand-rolled glob matcher; no new dependencies. Output gains an additive workspaces[] field; single-package projects return workspaces: []. Per-workspace scan aggregates libraries[]/totalCount/byScope by default. Back-compat: all 14 pre-existing tests pass byte-identical. Why: dogfood against ice-cola (pnpm monorepo, 7 package.json files, ~220 deps) revealed the prior implementation returned only 1 entry. How to apply: any peaks-cli workflow that depends on the library surface (peaks-rd preflight, breaking-changes cross-check) now sees the full monorepo surface, not just the root. Verified on ice-cola: totalCount 1 → 202, workspaces 0 → 7.
<!-- peaks-memory:end -->

<!-- peaks-memory:start kind=convention -->
The peaks CLI's reported version (peaks --version) is sourced from src/shared/version.ts, not package.json directly. The TS file is generated by scripts/sync-version.mjs, which is invoked by pnpm build but not by pnpm dev or by standalone tsx invocations. Why: this caused a real failure mode in slice B — the version was bumped to 1.2.9 in package.json but the CLI kept reporting 1.2.8 until the sync script ran. The slice B commit was amended to include the regenerated version.ts. How to apply: any future version-bump chore must run pnpm exec node scripts/sync-version.mjs (or pnpm build) and stage src/shared/version.ts in the same commit. A future improvement: promote sync-version.mjs to a predev/prepack hook so it always runs.
<!-- peaks-memory:end -->

<!-- peaks-memory:start kind=feedback -->
peaks-cli dogfood from source requires pnpm install before pnpm exec tsx src/cli/index.ts. A fresh clone ships a node_modules/ that is missing the runtime deps (chalk, ora, terminal-kit) because the dev mode runs via tsx rather than through the built dist. Symptom: ERR_MODULE_NOT_FOUND: chalk. Why: this blocked the round-2 dogfood on this session and was the #1 reported failure mode. How to apply: the README now documents the pnpm install step under "本地开发（从源码跑 CLI）". A future slice could add a predev script that runs pnpm install automatically, or add a clearer error message in src/cli/index.ts when chalk is missing. Global peaks install (npm install -g peaks-cli) bypasses this entirely.
<!-- peaks-memory:end -->

<!-- peaks-memory:start kind=lesson -->
Every peaks workspace init in the same peaks-cli session creates a new session directory (.peaks/2026-06-04-session-<6hex>/). The CLI does not reuse an existing valid session; it always auto-generates. This session bloat was observed in this session: 3 untracked dirs (c72760, 4f7f6e, 4ead08) appeared from intermediate workspace init calls, in addition to the 2 d65b45 and cda1cd dirs that were used for real work. Why: a side effect of the workflow and a real usability issue. The bloat also caused a downstream failure: peaks sc validate / peaks sc boundary reported valid:false / syncState:failed because the active session (4f7f6e) had no PRD/RD/QA artifacts; the cda1cd session held the artifacts but the binding didn't reach it. How to apply: a future slice should add a peaks workspace reconcile (or similar) command that consolidates the multiple session dirs into a single canonical one, AND fix the .peaks/.session.json binding to track the latest valid session instead of the first one created. Workaround for now: if SC commands need to validate, edit .peaks/.session.json manually to point to the session that holds the artifacts.
<!-- peaks-memory:end -->
