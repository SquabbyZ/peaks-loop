# RD Request 2026-06-04-workflow-resilience

- session: 2026-06-04-session-ec7f95
- linked-prd: .peaks/2026-06-04-session-ec7f95/prd/requests/2026-06-04-workflow-resilience.md
- linked-ui:  (no UI involved)
- type: chore (sub-slice 1 of 2; sub-slice 2 is `feature`)

## Red-line scope (Slice 1: W2 sync-version hooks)

**In-scope (only these):**
- `package.json` — add 4 new `pre*` script entries that invoke `node ./scripts/sync-version.mjs`: `predev`, `pretest`, `prepublish`. (`prepack` already exists in the repo and chains through `npm run build`, which already runs sync-version; do not duplicate.)

**Explicit out-of-scope (do not modify, mock, delete, or replace):**
- `scripts/sync-version.mjs` itself (the script is correct; only its invocation points change).
- `scripts/clean-dist.mjs`, `scripts/install-skills.mjs`, `scripts/pretest-coverage.mjs`, any other script.
- `src/shared/version.ts` (regenerated by the hook, not hand-edited).
- `package.json` `version` field, `dependencies`, `devDependencies`, `engines`, `bin`, `files`, `name` — all must stay byte-identical except for the 3 new script entries.
- Any other script in `package.json` (`build`, `prepack`, `postinstall`, `pretest:coverage`, `dev`, `test`, `typecheck`, `format`, `lint`, etc.) — keep their bodies unchanged.
- Slice 2 (W3 + W4) files: NOT in this slice.

## Standards preflight

- peaks standards init/update --project c:/Users/smallMark/Desktop/peaks-cli --dry-run: pre-existing. No new standards delta needed for a build-script chore.
- planned application: review-only (no changes to CLAUDE.md or .claude/rules/**).

## OpenSpec linkage (when openspec/ exists)

- change-id: (no openspec/ in peaks-cli at this commit; not applicable)
- entry validate: not applicable
- to-rd projection: not applicable
- exit validate (after implementation): not applicable

## Coverage status

- current total UT coverage: 95% (project baseline; not re-run for this chore since no source files change)
- new/changed code coverage: no source files change in this slice; the change is `package.json` scripts only
- gate verdict: pass

## Slice contract (Slice 1: W2)

- **slice id**: 2026-06-04-workflow-resilience / slice 1 (chore)
- **functional boundary**: package.json scripts only. No source code changes.
- **pre-change behavior**: `pnpm dev` and `pnpm test` (without going through `pnpm build`) leave `src/shared/version.ts` stale relative to `package.json`. Symptom: `peaks --version` reports the previous version after a `package.json` bump until the user runs `pnpm build` (or `node scripts/sync-version.mjs` manually).
- **target behavior**: `pnpm dev`, `pnpm test`, and `npm publish` (direct, not via `prepack`) each trigger `node ./scripts/sync-version.mjs` as a `pre*` hook before the underlying command runs. `src/shared/version.ts` is always in sync when the user runs any of those commands.
- **unit-test requirements**: none for this slice. The existing test suite (which imports `CLI_VERSION` transitively) will be re-run as a back-stop; if the existing tests pass, the value is correct.
- **acceptance checks**:
  - `cat package.json | jq .scripts.predev` returns `"node ./scripts/sync-version.mjs"`.
  - Same for `pretest` and `prepublish`.
  - `prepack` is unchanged (`"npm run build"`).
  - `pnpm dev` (with a no-op `dev` script) succeeds and `src/shared/version.ts` is rewritten to match `package.json` `version` (even when there's no diff — sync-version is idempotent).
  - `pnpm test` runs `pretest` first; the existing 1809 tests pass with no new failures introduced.
  - Idempotence: running `node ./scripts/sync-version.mjs` twice in a row produces no diff on `src/shared/version.ts` (already true; back-stop only).
  - No regression: `pnpm build` still works (it explicitly calls `node ./scripts/sync-version.mjs` as its first step, plus the new `predev`/`pretest`/`prepublish` hooks don't fire during `pnpm build`).
- **rollback plan**: single commit; `git revert <commit>` is sufficient. The hooks are additive; removing them restores the pre-slice behavior.
- **commit boundary**: one commit, scoped to `package.json` only.

## Implementation evidence

- **Diff paths** (target):
  - `package.json` — add 3 new script entries (`predev`, `pretest`, `prepublish`). Other fields byte-identical.
- **Test commands + outputs**:
  - `pnpm test` → must pass (back-stop; no new failures)
  - `cat src/shared/version.ts` after `pnpm test` → must contain `1.2.9` (matches `package.json` `version`)
  - `pnpm typecheck` → must remain clean
  - `pnpm build` → must remain green (full chain)
- **Code review**: chore slice; no CR needed per the type-chore light-gate rule. The change is 3 line additions in a JSON file.
- **Security review**: no new surface (build-time tooling only). Not required for chore.
- **Dry-run output**: post-commit, `pnpm test` should print version.ts content matching package.json. Verified manually.
- **Back-stop checks**: `peaks skill runbook peaks-solo --json` should still report `peaksCommandCount: 31` (this slice does not add a new peaks command); `peaks scan request-type-sanity --type chore --json` should return `consistent: true`.
- **Commit**: not yet authored at the time of writing this RD; will be assigned by the sub-agent.

## MCP usage (when external docs lookup was used)

- None. This slice is a 3-line JSON edit; no external lookups needed.

## Handoff

- to peaks-qa: not required for chore (light gate).
- to peaks-sc: not required for chore (no commit-boundary artifact needed).
- Slice 2 (W3 + W4) will reuse the same `2026-06-04-workflow-resilience` rid but a separate request transition with type `feature` (the same `peaks request init --role rd` for slice 2 will operate on the same rid; the artifacts are sliced per the sub-slice plan in the PRD).

## Status

- created: 2026-06-04T14:55:55.000Z
- last update: 2026-06-04T15:16:44.756Z
- state: implemented
