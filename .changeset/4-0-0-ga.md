---
"peaks-loop": major
---

4.0.0 GA release: v2.13.0 zero-pause contract closure + 24h mode integration + 11-rid batch (rid-020b through rid-034).

This is the GA cutover from the 4.0.0-beta.N line. The full changelog is in `CHANGELOG.md` under `## 4.0.0 — 2026-07-28 (GA release)`:

- 5 super-command CLI surface (`code / audit / doctor / openspec / release / release-pack`)
- peaks changeset check hard gate (rid-011)
- peaks release precheck 4-layer gate (rid-010)
- Dynamic workspace subpackage discovery (rid-014)
- Monorepo-wide version lockstep (rid-015)
- GitHub Release step + tag/version gate (rid-017)
- 5 pure-internal sub-packages folded into `src/services/*` (rid-016)
- CLI_VERSION drift closed (publish.yml gate-cli-version)
- 33→35 npm version-skip regression closed
- `workspace:*` protocol leak closed
- shared auto-bump no longer requires env gate (sync-version.mjs)

Plus 12 unmerged rids (rid-020b through rid-034): 24h mode integration, code-commands split, heartbeat watch, monotonic jsonl-store, auto-compact partial mode, context spillover storage, DAG wave + barrier, dashboard summary, dispatcher deprecation, session spill-demo CLI, spillover-store lstat defense-in-depth, and v2.13.0 contract cleanup (this slice).

Breaking change: the 73 pre-4.0.0 leaf CLI commands are no longer present on `$PATH`. See `docs/cli-migration-4-0-0.md` for the migration matrix.